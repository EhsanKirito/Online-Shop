<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="180dp"
    android:layout_height="220dp"
    android:background="#00FFD8"
    android:layout_margin="10dp">

    <ImageView
        android:id="@+id/categoryPoster"
        android:layout_width="150dp"
        android:layout_height="150dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_launcher_foreground" />

    <TextView
        android:id="@+id/txtName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="name"
        app:layout_constraintEnd_toEndOf="@+id/categoryPoster"
        app:layout_constraintStart_toStartOf="@+id/categoryPoster"
        app:layout_constraintTop_toBottomOf="@+id/categoryPoster" />
</androidx.constraintlayout.widget.ConstraintLayout>